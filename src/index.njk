---
layout: default.njk
---

<form>
  {% for route in routes %}
    <div id="{{ route.path }}" class="view">
      {% include "routes/" + route.path + ".njk" ignore missing %}
    </div>
  {% endfor %}
</form>
